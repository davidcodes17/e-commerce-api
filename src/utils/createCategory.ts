import Category from "../models/Category";

export default (categories: string /** SEPERATED BY COMMA **/) => {
  // const category = categories.trim().split(",");
  // category.map(async(ca)=>{
  //     const CategoryDB = await Category.bulkCreate({
  //     })
  // })
};
